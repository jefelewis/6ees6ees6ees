// Extend the Grub Methods to the Bee
class Bee extends Grub{

  // Constructor
  constructor(){
    // Super Calls the parent function, depending on the context, and then returns the result
    super();
    this.age = 5;
    this.color = "yellow";
    this.job = "Keep on growing";
  }

  // METHODS
}
